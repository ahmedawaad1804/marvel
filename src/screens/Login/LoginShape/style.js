import {StyleSheet} from 'react-native';
import * as scaling from 'scaling';

const styles = StyleSheet.create({
  Login_Shape:{
    position:'absolute',
    start:scaling.scale(150)

  }
})

export default styles
